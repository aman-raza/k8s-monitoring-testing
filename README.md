# k8s-monitoring-testing
Deployment of a minimal monitoring stack on Kubernetes: Prometheus + Grafana + a sample app that exposes Prometheus metrics. Add a testing Job (k6 or curl-based) to run synthetic tests, have Prometheus scrape app and job
